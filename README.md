# Лабораторная 1: Docker и Docker Compose

## Структура:

### `/docker`

- `Dockerfile.bad` — пример Dockerfile с 5 bad practices.
- `Dockerfile.good` — исправленный Dockerfile.
- `README.md` — описание bad practices в Dockerfile и 2 bad practices при работе с контейнерами.

### `/docker-compose`

- `docker-compose.bad.yml` — пример docker-compose файла с 5 bad practices.
- `docker-compose.good.yml` — исправленный docker-compose.
- `README.md` — описание bad practices в docker-compose и объяснение сетевой изоляции сервисов.
